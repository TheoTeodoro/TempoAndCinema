@model TempoAndCinema.Dtos.TmdbSearchResponseDto

@{
    ViewData["Title"] = "Buscar Filmes";
}

<<<<<<< HEAD
<div class="container mt-4">

    <!-- Barra de pesquisa -->
    <form method="get" asp-controller="Tmdb" asp-action="Search" class="d-flex mb-4">
        <input type="text" name="query" value="@Model?.Query" class="form-control me-2"
               placeholder="Buscar filmes..." style="border-radius: 0.5rem; padding: 10px;" />
        <button type="submit" class="btn btn-primary" style="border-radius: 0.5rem;">🔍 Buscar</button>
    </form>

    @if (Model?.Results != null && Model.Results.Any())
    {
        <h3 class="text-light mb-3">Resultados para "@Model.Query"</h3>

        <div class="row row-cols-1 row-cols-sm-2 row-cols-md-3 row-cols-lg-4 g-4">

            @foreach (var movie in Model.Results)
            {
                <div class="col">
                    <div class="card h-100 bg-dark text-light shadow-sm">
                        @if (!string.IsNullOrEmpty(movie.Poster_Path))
                        {
                            <img class="card-img-top rounded"
                                 style="height: 320px; object-fit: cover;"
                                 src="@($"https://image.tmdb.org/t/p/w500{movie.Poster_Path}")"
                                 alt="@movie.Title" />
                        }
                        else
                        {
                            <div class="card-img-top bg-secondary d-flex align-items-center justify-content-center"
                                 style="height: 320px; border-radius: 0.5rem;">
                                Sem imagem
                            </div>
                        }

                        <div class="card-body d-flex flex-column justify-content-between">
                            <h5 class="card-title text-truncate">@movie.Title</h5>
                            <p class="card-text text-muted">
                                @movie.Release_Date
                            </p>
                            <a class="btn btn-warning mt-2" 
                               asp-action="Details" 
                               asp-route-id="@movie.Id">
                                Ver detalhes
                            </a>
                        </div>
                    </div>
                </div>
            }

        </div>

        <!-- Paginação -->
        <div class="mt-4 d-flex justify-content-between align-items-center">
            @if (Model.Page > 1)
            {
                <a class="btn btn-outline-light"
                   asp-action="Search"
                   asp-route-query="@Model.Query"
                   asp-route-page="@(Model.Page - 1)">
                    ← Anterior
                </a>
            }
            else
            {
                <div></div>
            }

            <span>Página @Model.Page de @Model.TotalPages</span>

            @if (Model.Page < Model.TotalPages)
            {
                <a class="btn btn-outline-light"
                   asp-action="Search"
                   asp-route-query="@Model.Query"
                   asp-route-page="@(Model.Page + 1)">
                    Próxima →
                </a>
            }
            else
            {
                <div></div>
            }
        </div>

    }
    else if(!string.IsNullOrEmpty(Model?.Query))
    {
        <p class="text-light mt-3">Nenhum resultado encontrado para "@Model.Query".</p>
    }
</div>

<style>
    body {
        background-color: #111;
    }
    .card {
        transition: transform 0.3s, box-shadow 0.3s;
        cursor: pointer;
    }
    .card:hover {
        transform: scale(1.05);
        box-shadow: 0 10px 20px rgba(0,0,0,0.7);
    }
    h3 {
        color: #fff;
    }
    .btn-outline-light {
        color: #fff;
        border-color: #fff;
    }
    .btn-outline-light:hover {
        color: #111;
        background-color: #fff;
        border-color: #fff;
    }
</style>
=======
<h2>Buscar Filmes</h2>

<form method="get" asp-controller="Tmdb" asp-action="Search">
    <input type="text" name="query" value="@Model?.Query" placeholder="Digite um filme..." />
    <button type="submit">Buscar</button>
</form>


@if (Model?.Results != null && Model.Results.Any())
{
    <h3>Resultados para "@Model.Query"</h3>

    <div class="row row-cols-1 row-cols-md-4 g-4 mt-2">

        @foreach (var movie in Model.Results)
        {
            <div class="col">
                <div class="card h-100 shadow-sm">
                    
                    @if (!string.IsNullOrEmpty(movie.Poster_Path))
                    {
                        <img class="card-img-top"
                             style="height: 250px; object-fit: cover; border-radius: 0.5rem;"
                             src="@($"https://image.tmdb.org/t/p/w500{movie.Poster_Path}")"
                             alt="@movie.Title" />
                    }
                    else
                    {
                        <div class="card-img-top bg-secondary text-white d-flex align-items-center justify-content-center"
                             style="height: 250px; border-radius: 0.5rem;">
                            Sem imagem
                        </div>
                    }

                    <div class="card-body">
                        <h5 class="card-title">@movie.Title</h5>
                        <p class="card-text">
                            @movie.Release_Date
                        </p>
                    </div>
                    <a class="btn btn-primary mt-2"
                       asp-action="Details"
                       asp-route-id="@movie.Id">
                        Ver detalhes
                    </a>


                </div>
            </div>
        }

    </div>

    <div class="mt-4 d-flex justify-content-between align-items-center">
        @if (Model.Page > 1)
        {
            <a class="btn btn-outline-primary"
               asp-action="Search"
               asp-route-query="@Model.Query"
               asp-route-page="@(Model.Page - 1)">
                ← Anterior
            </a>
        }

        <span>Página @Model.Page de @Model.TotalPages</span>

        @if (Model.Page < Model.TotalPages)
        {
            <a class="btn btn-outline-primary"
               asp-action="Search"
               asp-route-query="@Model.Query"
               asp-route-page="@(Model.Page + 1)">
                Próxima →
            </a>
        }
    </div>
}
>>>>>>> origin/LucayanBranch
